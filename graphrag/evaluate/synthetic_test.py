# Copyright (c) 2024 Microsoft Corporation.
# Licensed under the MIT License

"""Generate synthetic test data to support CLI."""

import pandas as pd

def generate_synthetic_data(doc_df: pd.DataFrame) -> pd.DataFrame:
    # return the output dataframe with the following columns:
    #   - doc_id: unique identifier for each document
    #   - query: the question to be answered
    #   - answer: the answer to the question
    pass
